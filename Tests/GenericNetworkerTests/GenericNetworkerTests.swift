import XCTest
@testable import GenericNetworker

final class GenericNetworkerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GenericNetworker().text, "Hello, World!")
    }
}
